export default [{
  path: '/timeline',
  component: require('./Timeline')
  // getComponent(location, cb) {
  //   require.ensure([], (require) => {
  //     cb(null, require('./Dashboard'));
  //   });
  // }
}];
